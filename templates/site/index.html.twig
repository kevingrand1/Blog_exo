{% extends ('base.html.twig') %}

{% block body %}

<h1>Le blog</h1>
<hr>

<section>
    <h4>Tout les posts</h4>
    {% include 'utils/_search_bar.html.twig' %}
    {% include ('utils/_show_post.html.twig') %}
    <div class="d-flex justify-content-center mt-5">
        {{ knp_pagination_render(posts) }}
    </div>
</section>

{% endblock %}