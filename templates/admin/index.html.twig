{% extends ('base_admin.html.twig') %}

{% block body %}

<div class="container">
    <div class="pt-5 mb-4">
        {% include ('utils/alert.html.twig') %}
        <h1 class="mb-4">Administration  <a href="{{ path('admin_add_post') }}" class="badge badge-pill alert-primary text-decoration-none fs-4">Créer un post</a></h1>
        <hr>
    </div>

    <section class="mt-5">
        <h2 class="mb-3">Historique des posts</h2>
<a href="{{ path('admin_show_categories') }}" class="badge badge-pill alert-primary text-decoration-none fs-4">Voir catégories</a>
        {# boucle foreach post #}
        </div>
    </section>
</div>

{% endblock %}